<h1><i class="fas fa-rocket"></i> login to e-commerce wanzofc-tech</h1>
<% if (typeof message === 'string' && message) { %><p class="message-banner error"><%= message %></p><% } %>
<% if (typeof message === 'object' && message && message.text) { %><p class="message-banner <%= message.type || 'error' %>"><%= message.text %></p><% } %>

<form action="/auth/login" method="POST" style="max-width: 500px; margin: 2rem auto; padding: 2rem; background-color: var(--bg-secondary); border-radius: var(--border-radius-md); box-shadow: var(--shadow-md);">
    <div>
        <label for="email"><i class="fas fa-envelope"></i> Email Address:</label>
        <input type="email" id="email" name="email" placeholder="your.email@domain.com" required>
    </div>
    <div>
        <label for="password"><i class="fas fa-key"></i> Password:</label>
        <input type="password" id="password" name="password" placeholder="Enter your secure password" required>
    </div>
    <div class="text-center">
        <button type="submit" class="form-button"><i class="fas fa-sign-in-alt"></i> Engage Warp Drive (Login)</button>
    </div>
    <p class="form-link">No account? <a href="/auth/register">Create one in a parsec!</a></p>
</form>