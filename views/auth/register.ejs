<h1><i class="fas fa-user-plus"></i> mari bergabung untuk berbisnis bersama</h1>
<% if (typeof message === 'string' && message) { %><p class="message-banner error"><%= message %></p><% } %>
<% if (typeof message === 'object' && message && message.text) { %><p class="message-banner <%= message.type || 'error' %>"><%= message.text %></p><% } %>

<form action="/auth/register" method="POST" style="max-width: 600px; margin: 2rem auto; padding: 2rem; background-color: var(--bg-secondary); border-radius: var(--border-radius-md); box-shadow: var(--shadow-md);">
    <div>
        <label for="username"><i class="fas fa-user-astronaut"></i> Callsign (Username):</label>
        <input type="text" id="username" name="username" placeholder="Your unique username" required>
    </div>
    <div>
        <label for="email"><i class="fas fa-at"></i> Comms Channel (Email):</label>
        <input type="email" id="email" name="email" placeholder="your.valid@email.system" required>
    </div>
    <div>
        <label for="password"><i class="fas fa-shield-alt"></i> Access Code (Password):</label>
        <input type="password" id="password" name="password" placeholder="Choose a strong password" required>
    </div>
    <div>
        <label for="confirmPassword"><i class="fas fa-sync-alt"></i> Confirm Access Code:</label>
        <input type="password" id="confirmPassword" name="confirmPassword" placeholder="Re-enter your password" required>
    </div>
    <div>
        <label for="role"><i class="fas fa-user-tag"></i> Register as:</label>
        <select name="role" id="role">
            <option value="buyer" selected>Galactic Shopper (Buyer)</option>
            <option value="seller">Interstellar Merchant (Seller)</option>
        </select>
    </div>
    <div id="whatsappField" style="display:none;">
        <label for="whatsappNumber"><i class="fab fa-whatsapp"></i> Subspace Comms (WhatsApp Number e.g., 628123...):</label>
        <input type="text" id="whatsappNumber" name="whatsappNumber" placeholder="Format: 628xxxxxxxxxx">
    </div>
    <div class="text-center">
        <button type="submit" class="form-button"><i class="fas fa-user-check"></i> Finalize Registration</button>
    </div>
     <p class="form-link">Already a member? <a href="/auth/login">Login to your starship!</a></p>
</form>